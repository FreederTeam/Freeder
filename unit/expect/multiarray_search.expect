multiarray_search
If not found, return false:          bool(true)
If not found, return default_value:  bool(true)
If found, return sub_array:          bool(true)

multiarray_search_key
If not found, return -1:             bool(true)
If found, return sub_array key:      bool(true)

multiarray_filter
If never found, return multiarray:   bool(true)
If found, doesn't return multiarray: bool(true)
But can be completed:                bool(true)
